apiVersion: kaptain.org/1.29
metadata:
  description: "GitHub release How to Consume short-first layer"

spec:
  platform:
    githubActions:
      release:
        consumerReferenceForms: short-first
